#include "main.h"
/**
 *	print_most_numbers - function that print number from 0 to 9
 *	Return: 0 t0 9 except 2 and 4
 */
void print_most_numbers(void)
{
	int i;

	{
	for (i = 0; i <= 9 ; i++)
		if ((i == 2) || (i == 4))
			cotinue;
	}
	else
	{
		putchar(i + '0')
	}
		putcher('\n')
}
