#include "main.h"
/**
 *	_isupper - function that check for upper case character
 *	0c:input
 *	Return: 1 if c is the upper case, 0 otherwise
 */
int _isupper(int c)
{
	
	if ((c >= 'A') && (c <= 'Z'))
	return (1);

	else
	
	return (0);	
}
